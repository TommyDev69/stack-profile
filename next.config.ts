import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  reactStrictMode: true,
};

export default nextConfig;



// // next.config.ts
// import type { NextConfig } from 'next';

// const nextConfig: NextConfig = {
//   reactStrictMode: true,  // Enable React Strict Mode for better debugging in development
//   experimental: {
//     appDir: true,  // Enable App Router (app directory-based routing)
//   },
//   images: {
//     domains: ['example.com'], // You can configure external image domains if needed
//   },
// };

// export default nextConfig;
